/**
 * src/js/config.example.js
 *
 * Template for config.js — safe to commit.
 * Copy this file to config.js and fill in the values.
 *
 *   cp src/js/config.example.js src/js/config.js
 *
 * config.js is listed in .gitignore and must never be committed.
 * On Vercel, values are injected at build time as environment variables
 * and written into config.js by vercel-build.sh.
 */

const CONTRACT_ADDRESS = 'YOUR_DEPLOYED_CONTRACT_ADDRESS_HERE';
const NETWORK_ID       = 11155111; // 11155111 = Sepolia, 17000 = Holesky
